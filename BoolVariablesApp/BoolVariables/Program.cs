

// bool can have true or false states
// by default it sets to false

bool isComplete = false;

// flip boolean
isComplete = !isComplete;

Console.WriteLine(isComplete);
// Console.WriteLine(isComplete.ToString());